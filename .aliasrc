f='cd ~/projects/aalto-co/frontend && echo "docker-compose up"'
f2='cd ~/Projects/aalto-co/frontend && echo "npm run build:watch"'
b='cd ~/Projects/aalto-co/backend && echo "npm run start:dev"'
ap='cd ~/Projects/aalto-co/new/pubsite && PORT=3030 npm run dev'
p1='cd ~/projects/aalto-co/pwa && npm run dev && open http://localhost:3000'
p2='cd ~/projects/aalto-co/pwa && code .'

du='du -sh'
diff='diff -u'
grep='grep --color=auto'
rgrep='grep -r --color=auto'
mk='mkdir -p'
o=open
rrm='rm -rf'
datefn='date +"%Y.%m.%d-%M.%S"'

# ls
ls='ls -FG'
l='ls -lh'
l='ls -lh'
ll='ls -lh'
la='ls -A'
lla='la -lhA'

# Arch
pac=pacman
y=yaourt
ins='y -S'
rem='y -Rs'
net='sudo netcfg2 -a; sudo netcfg2'
x=startx

# git
g=git
gco='git checkout'
gg='git get'
ga='git add'
gd='git diff'
gs='git status'
gst='git stash'
gsta='git stash apply'
gci='git commit'
gl='git log --abbrev-commit'
glp='git log -p'
gv='git svn'
gp='git pull && git submodule update --recursive'
gr='git rebase'
gsq='git rebase -i head~5'
gb='git branch'
gbl='git for-each-ref --sort=-committerdate refs/heads/ | less'
#git='hub'
gcaca='ga . && gci -a -C HEAD --amend'
goo='git open'
gu='git url'
gx='git examine'
gpom="git checkout master && git pull && git submodule update && git submodule --quiet foreach 'git submodule --quiet sync && git submodule update --init --recursive'"
gps='git pull && git push origin head && git checkout develop && git pull && git merge - && git push origin head && git checkout -'

mate='code'

# nose
nose=nosetests
#n=nose
#nh='nose -h M'

# Python
py=python
ip=ipython
ipo=ipython
pyc='rm -rfv **/*.pyc'
json='python -m json.tool'

# Clipboard
pbj='pbpaste | json'
pbjc='pbj | pbcopy'

# vim
v=vim
#c=v
#b=v

# vim: set ft=sh
