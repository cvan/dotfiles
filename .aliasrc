du='du -sh'
diff='diff -u'
grep='grep --color=auto'
rgrep='grep -r --color=auto'
mk='mkdir -p'
p=popd
o=open
rrm='rm -rf'

check=check.py
django='python manage.py'

# ls
ls='ls -FG'
l='ls -lh'
l='ls -lh'
ll='ls -lh'
la='ls -A'
lla='la -lhA'


# Arch
pac=pacman
y=yaourt
ins='y -S'
rem='y -Rs'
net='sudo netcfg2 -a; sudo netcfg2'
x=startx

# git
g=git
gco='git checkout'
gg='git get'
ga='git add'
gd='git diff'
gs='git status'
gst='git stash'
gsta='git stash apply'
gci='git commit'
gl='git log --abbrev-commit'
glp='git log -p'
gv='git svn'
gp='git pull && git submodule update --recursive'
gr='git rebase'
gsq='git rebase -i head~5'
gb='git branch'
#git='hub'
gcaca='gci -a -C HEAD --amend'
go='git open'
gu='git url'
gx='git examine'
gpom='git push origin master && sleep 30 && python scripts/checkdev.py&'

mate='subl'

# nose
nose=nosetests
n=nose
nh='nose -h M'

# Python
py=python
ip=ipython
ipo=ipython
pyc='rm -rfv **/*.pyc'
json='python -m json.tool'

# Django
z='cd /opt/zamboni && workon z'
zpull="z && git checkout master && git pull && git submodule update && git submodule --quiet foreach 'git submodule --quiet sync && git submodule update --init --recursive'"

zup='zpull && schematic migrations'
check5="git whatchanged $1 -1 | awk '/^:/ {print $NF}' | xargs check.py"

testme='manage.py test --noinput -s --logging-clear-handlers -a\!sphinx --verbosity=2'
tm='testme --settings=settings_local_mkt'
ta='testme'

# vim
v=vim
#c=v
#b=v

# Projects
pr='cd ~/Sites/projects/prana && workon prana'

# ssh targets
d='ssh dekkostudios.com@dekkostudios.com'
p='ssh cwiemeersch@people.mozilla.org'

# vim: set ft=sh
